import { SortPhoneNumbPipe } from './sort-phone-numb.pipe';

describe('SortPhoneNumbPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPhoneNumbPipe();
    expect(pipe).toBeTruthy();
  });
});
